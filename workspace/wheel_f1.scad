// Parametric F1-Style Wheel

// ---------------------- Parameters ----------------------
outer_diameter = 70;        // mm, outer diameter of tire
tire_width = 40;            // mm, width of tire
tire_thickness = 12;        // mm, radial tire wall thickness
rim_diameter = 48;          // mm, rim diameter (where rim meets tire inside)
rim_width = 36;             // mm, outer width of rim
rim_depth = 7;              // mm, depth rim is inset from tire face
hub_diameter = 18;          // mm, central hub diameter
hub_width = 36;             // mm, width of hub (matches rim base)
hub_axle_hole = 10;         // mm, center hole for axle
num_spokes = 10;            // number of spokes
spoke_radius = 4;           // mm, radius of spokes
spoke_embed_rim = 2;        // mm, how much spokes are embedded into rim
spoke_embed_hub = 2;        // mm, how much spokes are embedded into hub
$fn = 128;                  // Smoothness for ci...